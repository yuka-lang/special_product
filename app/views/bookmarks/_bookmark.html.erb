    <% if post.bookmarked_by?(current_user) %>
      <td><%= link_to "ブックマークを外す", post_bookmarks_path(post), method: :delete, remote: true %></td>
    <% else %>
      <td><%= link_to "ブックマークに追加する", post_bookmarks_path(post), method: :post, remote: true %></td>
    <% end %>