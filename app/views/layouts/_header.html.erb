    <nav class="navbar navbar-expand-lg navbar-light">
      <div class="container">
        <h2 style="font-family: cursive;"><span>ロゴ</span></h2>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
              <!--検索フォーム-->
              <%= form_with(url: search_posts_path, local: true, method: :get, class: "search-form  nav-item") do |f| %>
                <li class="m-3">
                  <%= f.text_field :keyword, placeholder: "キーワードで検索する", class: "search-input nav-item" %>
                  <%= f.submit "検索", class: "search-btn" %></li>
              <% end %>
              <% if user_signed_in? %>
                <li><%= link_to 'マイページ', user_path(current_user.id), class: "btn btn-danger m-2 rounded nav-item" %></li>
                <li><%= link_to 'TOP', posts_path, class: "btn btn-danger m-2 rounded nav-item" %></li>
                <li><%= link_to '会員一覧', users_path, class: "btn btn-danger m-2 rounded nav-item" %></li>
              <% else %>
                <li><%= link_to 'TOP', posts_path, class: "btn btn-danger m-2 rounded nav-item" %></li>
                <li><%= link_to "新規登録", new_user_registration_path, class: "btn btn-danger m-2 rounded nav-item" %></li>
                <li><%= link_to "ログイン", new_user_session_path, class: "btn btn-danger m-2 rounded nav-item" %></li>
              <% end %>
             </ul>
          </div>
      </div>
    </nav>