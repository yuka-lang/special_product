<p>マイページ（会員詳細画面）</p>
<div>
  プロフィール画像：<%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %></p>
  <p>名前:<%= @user.name %></p>
  <p>紹介文：<%= @user.introduction %></p>
  <% if @user.id == current_user.id %>
    <p><%= link_to "プロフィール編集", edit_user_path(@user) %></p>
    <p><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></p>
    <p><%= link_to "新規投稿", new_post_path(@user) %></p>
  <% end %>
</div>

<h1>私の投稿一覧</h1>
<% @posts.each do |post| %>
  <div>
    <%= link_to post_path(post.id) do %>
      <%= attachment_image_tag post, :image, :fill, 200, 200, format: 'jpeg', fallback: "no_image.jpg" %>
    <% end %>
    <p>タイトル：<%= post.title %></p>
  </div>
<% end %>
<%= paginate @posts %>