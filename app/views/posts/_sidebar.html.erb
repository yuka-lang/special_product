  <!--タグ一覧-->
  <% tag_lists.each do |list| %>
      <!--タグが0の時は表示しない-->
      <% if list.posts.count != 0 %>
        <%=link_to list.name,search_tag_path(tag_id: list.id) %>
        <%="(#{list.posts.count})" %>
      <% end %>
    <% end %>

  <h4>今月のおすすめ</h4>
    <div>
      <!--コメントの多い順位(1位から順に)一覧を表示する-->
      <% month_post_comment_ranks.each.with_index(1) do |post, i| %>
       第<%= i %>位
      <%= link_to post_path(post.id) do %>
        <%= attachment_image_tag post, :image, :fill, 220, 150, format: 'jpeg', fallback: "no_image.jpg" %>
    <p class="card-title"><%= post.title %></p>
      <% end %>
    <!--いいねカウント数-->
    <span class="ml-5">❤️️<%= post.favorites.count %></span>
    <!--投稿一覧画面で投稿に紐づくタグを表示-->
    <i class="fas fa-tag">
      <% post.tags.each do |tag| %>
        <%= link_to tag.name,search_tag_path(tag_id: tag.id)%>
        <%="(#{tag.posts.count})" %>
      <% end %></i>

      <% if user_signed_in? %>
        <p><%= link_to post.comments.count, post_path(post.id) %>コメント</p>
      <% end %>
    <% end %>

  <h4>今週のいいねランキング</h4>
    <div>
      <!--ランキングと順位(1位から順に)一覧を表示する-->
      <% week_post_favorite_ranks.each.with_index(1) do |post, i| %>
       第<%= i %>位
      <%= link_to post_path(post.id) do %>
        <%= attachment_image_tag post, :image, :fill, 220, 150, format: 'jpeg', fallback: "no_image.jpg" %>
    <p class="card-title"><%= post.title %></p>
      <% end %>
    <!--いいねカウント数-->
    <span class="ml-5">❤️️<%= post.favorites.count %></span>
    <!--投稿一覧画面で投稿に紐づくタグを表示-->
    <i class="fas fa-tag">
      <% post.tags.each do |tag| %>
        <%= link_to tag.name,search_tag_path(tag_id: tag.id)%>
        <%="(#{tag.posts.count})" %>
      <% end %></i>

      <% if user_signed_in? %>
        <p><%= link_to post.comments.count, post_path(post.id) %>コメント</p>
      <% end %>
    <% end %>
  </div>